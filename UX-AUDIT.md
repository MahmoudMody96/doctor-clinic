# 📋 تقرير تقييم UI/UX شامل — موقع عيادة الشريف

> تقييم احترافي بمنهجيات: Heuristic Evaluation (Nielsen) + WCAG 2.2 AA + تحليل RTL + CRO + فحص SEO/Meta
> **النطاق:** الصفحة الرئيسية + قمع الحجز (5 خطوات) + لوحة التحكم
> **التقييم الكلي: 6.6/10** — أساس هندسي وبصري ممتاز يسبق مستوى الناضجين في الإتاحة والثقة

## جدول النتائج

| المحور | الدرجة /10 | تعليل |
|---|---|---|
| Heuristic Evaluation (Nielsen) | **8.1** | حالة النظام والأخطاء ممتازة؛ الضعف في المرونة (لا حفظ مسودة) والسيطرة للمريض بعد الحجز |
| Accessibility (WCAG 2.2 AA) | **4.0** | تباين ٦ أزواج فاشلة + focus مفقود + reduced-motion غائب + touch targets ناقصة |
| التخصص العربي RTL | **8.0** | خصائص منطقية و`dir="ltr"` صحيحة للأرقام؛ التسلسل الرقمي عربي/لاتيني غير متسق |
| قمع التحويل (CRO) | **6.5** | هيكل ممتاز بثقة مدمجة؛ الاحتكاك: إجازات متأخرة، لا حفظ حالة، شاشة نجاح ناقصة |
| التصميم البصري والاتساق | **7.5** | tokens منضبطة وحالات كاملة؛ نصوص 10-11px، انجراف tokens في الإدارة |
| Responsive & Mobile | **8.0** | كروت بدل الجداول وStepper مختصر؛ زر الحجز مخفي < 640px وفتحات 41px |
| الثقة والخصوصية (طبي) | **5.0** | لا سياسة خصوصية، لا صور حقيقية، تقييمات بلا مصدر |
| SEO & Social & Meta | **6.0** | عناوين SSR سليمة؛ صفر og/twitter/canonical/robots/sitemap/JSON-LD |

## الملخص التنفيذي

موقع بمستوى تنفيذ بصري أعلى من المتوسط السوقي المصري بوضوح: نظام تصميم حقيقي، لغة بصرية متسقة، حالات تحميل/أخطاء عربية ممتازة، وقمع حجز مبني بعناية CRO واضحة. لكن التنفيذ يخسر نقاطًا في ثلاث جبهات:

1. **إتاحة الوصول ضعيفة**: لا `focus-visible` في المشروع كله، صفر احترام لـ `prefers-reduced-motion`، وستة أزواج ألوان تحت حد 4.5:1 (أخطرها أبيض على brand-500 = 2.49:1 في زر الحجز الرئيسي نفسه)
2. **الثقة الطبية ناقصة**: صفر صور حقيقية (لا طبيب ولا عيادة ولا قبل/بعد)، لا صفحة خصوصية أو سياسة إلغاء
3. **قمع الحجز**: أيام الإجازة تُكتشف متأخرًا (الجمعة يُختار ثم يُرفض) ولا حفظ للحالة عند التحديث

---

## 🔴 P0 — يضر المستخدم/التحويل فورًا

### P0-1 | تركيز لوحة المفاتيح معدوم في قمع الحجز
- `grep focus-visible src/` = 0 نتائج. `StepDetails.tsx:22`: `focus:outline-none focus:ring-brand-100` — ring شبه غير مرئي (تباين ≈ 1.1:1). بطاقات الخدمة/الأيام/الأوقات بلا أي focus style.
- **إصلاح:** `:where(a, button, input, textarea, [tabindex]):focus-visible { outline: 2px solid var(--color-brand-700); outline-offset: 2px; }` في globals.css + استبدال ring-brand-100 بـ brand-500.

### P0-2 | زر الإجراء الرئيسي فاشل التباين — أبيض على brand-500 = 2.49:1
- `btn-primary` (globals.css:135) تدرج brand-600→500. نفس التدرج في Navbar:74 وStepTime:246 وStepDate:64 وadmin/layout:105.
- **إصلاح:** التدرج يصبح `from-brand-800 to-brand-700` (4.8:1+) أو نص داكن `text-brand-950`.

### P0-3 | صفر صور حقيقية — ثقة طبية مؤسِّسة غائبة
- صفر وسم `<img>` في الموقع كله. عيادة **تجميل** أسنان تبيع نتيجة بصرية بلا صورة قبل/بعد واحدة.
- **إصلاح:** قسم Before/After بعد WhyUs + صورة الطبيب الحقيقية في About + ٢-٣ صور عيادة في Contact.

### P0-4 | يوم الإجازة يُكتشف في خطوة ٣ بعد اختياره في خطوة ٢
- StepDate يعرض ١٢ يومًا كلها متاحة بصريًا؛ الجمعة يُرفض دائمًا في الخطوة التالية.
- **إصلاح:** تعطيل رقائق أيام الإجازة مباشرة (`opacity-50` + شارة "مغلق" + aria-disabled) عبر خريطة أيام العمل من الجدول.

### P0-5 | لا حفظ لحالة المعالج — التحديث يمسح كل شيء
- كل الحالة في useState فقط؛ pull-to-refresh بعد الخطوة ٤ = فقدان كل شيء.
- **إصلاح:** sessionStorage (مفتاح `booking-draft`) + استرجاع مع Toast "استعدنا اختياراتك" — أقل من ٣٠ سطرًا.

---

## 🟠 P1 — مهم

### P1-1 | ستة أزواج تباين فاشلة (محسوبة)
| الزوج | النسبة | المطلوب | مواضع |
|---|---|---|---|
| ink-400 على أبيض | 2.91 | 4.5 | تلميحات الحقول، ملخص الحجز، شاشة النجاح |
| ink-300 على أبيض | 1.87 | 4.5 | عدّاد الملاحظات |
| ink-500 على أبيض | 4.41 | 4.5 | عناوين التواصل، نقاط الثقة، دور المقيّم |
| gold-400 على brand-600 | 2.24 | 3.0 | كلمة "الأفضل" في CtaBanner |
| ink-600 على ink-950 | 2.58 | 4.5 | رابط "دخول الإدارة" |
| نجوم gold على أبيض | 1.67-2.15 | 3.0 | Testimonials، Hero |

**إصلاح:** ink-400→ink-600 للتلميحات، ink-300→ink-500 للعدّاد، "الأفضل" تصبح بيضاء بخط سفلي ذهبي.

### P1-2 | `prefers-reduced-motion` غير محترم نهائيًا
```css
@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; }
}
```

### P1-3 | أنماط `role="radio"` بلا تنفيذ سهمي
StepService/StepDate/StepTime تستخدم role="radio" لكنها أزرار عادية بلا أسهم. **الإصلاح الأبسط:** استبدال بـ `aria-pressed` + aria-label وصفي.

### P1-4 | المودالات بلا focus trap ولا تركيز ابتدائي
Modal.tsx يتعامل مع Escape فقط. **إصلاح:** focus() عند الفتح + حلق Tab + aria-labelledby.

### P1-5 | لا Skip link
٨ ضغطات Tab قبل المحتوى بسبب النافبار fixed. **إصلاح:** ٣ أسطر — رابط "تخطَّ إلى المحتوى" + id="main".

### P1-6 | شاشة النجاح ناقصة إغلاق الحلقة
ينقص: إضافة للتقويم (ICS بلا مكتبات — ٢٠ سطرًا)، عنوان العيادة/خريطة، وعد تأكيد واتساب.

### P1-7 | صفر Meta اجتماعية وStructured Data
لا og:/twitter:/canonical/robots.txt/sitemap.xml/JSON-LD. **إصلاح:** openGraph+robots في layout.tsx + JSON-LD نوع Dentist + app/robots.ts + app/sitemap.ts.

### P1-8 | زر "احجز الآن" مخفي على الموبايل الصغير (< 640px)
أغلب ترافيك عيادات الأسنان موبايل 70-85%. **إصلاح:** زر مضغوط أو أيقونة دائرية دائمًا ظاهرة.

### P1-9 | زر "التالي" في الخطوة ٤ يبدو مفعلًا وهو غير صالح
**إصلاح:** حالة تحذيرية `opacity-60` + تمرير التركيز لأول حقل خاطئ بدل Toast التوبيخي.

### P1-10 | الخصوصية الطبية: وعود مبعثرة بلا صفحة سياسة
**إصلاح:** صفحة /privacy (متوافقة مع قانون حماية البيانات المصري 151/2020) + `robots: index:false` على admin/login.

---

## 🟡 P2 — تحسينات

1. **نصوص 10-11px للعربية** — العربي يفقد مقروئيته تحت 12px (Hero/StepService/BookingSummary/StepConfirm/Footer)
2. **Touch targets تحت 44px** — خانات الوقت ≈ 41px، دوائر Stepper 40px، أزرار الإدارة ≈ 32px
3. **تضارب أرقام عربي/لاتيني** — اختر نظامًا واحدًا
4. **بناء `!` بادئة v3 لا يعمل في v4** — `!px-5` → `px-5!` في ملفات admin
5. **انجراف tokens** — STATUS_COLORS تستخدم amber/teal/sky/rose بدل الهوية
6. **قفزات heading** — h1→h3 في Hero، h4 بلا h3 في StepTime/Footer
7. **أنيميشن من اليسار في RTL** — Navbar.tsx:109: `x: 16` → `x: -16`
8. **لا حقل بريد اختياري** — يفتح قناة تذكير T-24h
9. **keywords meta بلا قيمة** — احذفها + metadata لصفحة 404
10. **لا headers أمان** — HSTS/X-Frame-Options/X-Content-Type-Options في next.config

---

## ⚡ Quick Wins (كل واحدة < ساعة)

1. CSS الـ focus-visible العام — ١٠ أسطر
2. تغميق تدرج btn-primary إلى brand-800/700
3. استبدال درجات ink الفاتلة في مواضع P1-1
4. بلوك prefers-reduced-motion — ٦ أسطر
5. robots: index:false لـ admin/login + app/robots.ts
6. Skip link + id="main" — ٣ أسطر
7. استبدال role="radio" بـ aria-pressed
8. "الأفضل" الذهبية → بيضاء بخط ذهبي
9. زر واتساب ثابت في صفحة الحجز للهاربين من الفانل
10. خانات الوقت py-3 + min-h-[44px] + النجوم على خلفية داكنة

## 🎯 توصيات استراتيجية

1. **معرض قبل/بعد تفاعلي** — أعلى رافعة تحويل في تجميل الأسنان
2. **اختبار A/B**: 5 خطوات مقابل دمج (بياناتك+التأكيد) — فرضية +5-10% إكمال
3. **تحليلات وقمع أحداث** — GA4 + Meta Pixel + أحداث الخطوات (بلا قياس كل CRO تخمين)
4. **تأكيد وتذكير واتساب أوتوماتيكي** (T-24h وT-3h) — خفض no-show قابل للقياس
5. **صفحات خدمات فردية + Dentist schema** — SEO محلية
6. **اختبار قابلية استخدام** مع ٥ مصريين فوق ٤٥ سنة على موبايل

## 🛡️ ما لا يجب تغييره — نقاط قوة تحميها

1. نظام tokens المركزي (globals.css) — كل إصلاح تباين يعدّل القيم هنا لا ترقيع بكلاسات
2. النصوص العربية الطبيعية ("ساعة ونصف"، "موعدان") — تفصيلة إدراكية نادرة في مواقع عربية
3. Stepper الحالي (aria-current + نسخة موبايل) — نموذج تعليمي، طوّره ولا تستبدله
4. منظومة skeleton/empty/error مع retry — نضج نادر
5. استرجاع تعارض 409 (رجوع للخطوة ٣ بفتحات محدثة) — سلوك CRO احترافي
6. التحديث المتفائل مع تراجع في جدول الحجوزات
7. الخصائص المنطقية (ms/me/ps/pe) وdir="ltr" للأرقام — بنية RTL صحيحة من أساسها
8. تباين الأقسام الداكنة (9-11:1) — التوازن ناجح
9. التحول جدول→كروت للموبايل في الإدارة
10. رسائل الأخطاء العربية القابلة للتنفيذ — اجعلها المرجع لأي نص خطأ جديد

---

**الخلاصة:** الأساس الهندسي والبصري يستحق 8/10، لكن الموقع بوضعه الحالي يستبعد مستخدمي الكيبورد وضعاف البصر قانونيًا، ويبيع ابتسامات بلا صورة ابتسامة واحدة — تنفيذ الـ 10 Quick Wins + الـ 5 P0 (بضعة أيام عمل) يرفع التقييم الواقعي إلى 8+/10.
